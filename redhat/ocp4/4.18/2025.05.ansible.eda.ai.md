# integrate ansible platform with ai and eda

Redhat pulish an ai powered aap solution with serivce.now

<img src="imgs/2025.05.ansible.service.now.md/2025-05-29-11-04-48.png" heigth="600px">

source:
- https://labs.demoredhat.com/
- https://interact.redhat.com/share/kCuEEAIeU2a8plQcDALz

We will try to implement the pattern, and improve it to fit our needs.

## testing service.now call aap webhook

key point:
1. change request trigger workflow (in service now)
2. workflow approval (in service.now)
3. call aap workflow webhook (from service.now to aap)
4. pass parameter from service.now to aap workflow

![](files/aap.snow.01.drawio.png)

overall process:
1. On Service Now, define a workflow that, upon detecting an approved change request, remotely invokes a workflow template on AAP, passing a parameter named 'playbook to run'.
2. On AAP, define a workflow:
   1. Step 1: Run project-a's playbook, outputting the message "step1 ok".
   2. Step 2: Resync project-b.
   3. Step 3: Run project-b's playbook based on the workflow's parameter 'playbook to run', outputting the message "step3 ok".

### workflow webhook setting in aap

We define a workflow in aap, and enable `webhook`, with type `gitlab`, write down the `webhook url` and `webhook key`.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-19-34-01.png" heigth="600px">

Then we define the workflow, with 2 step, both are very simple ansible playbook, we will use this pattern for later advanced logic.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-19-34-24.png" heigth="600px">

the ansible source code:
- https://github.com/wangzheng422/ansible.service.now/blob/main/job.template/debug_message_step_01.yml
- https://github.com/wangzheng422/ansible.service.now/blob/main/job.template/debug_message_step_02.yml

The keypoint of the source code, is how it get extra vars from `service.now`:
```yaml
  vars:
    playbook_to_run: "{{ awx_webhook_payload.gitlab_payload.playbook_to_run | default('default_playbook_value') }}"
```

We have a webhook, we test it out with a `curl` command. Through the `curl` command, we can get the payload and headers needed during the webhook call.

> [!NOTE]
> change the project.id to correct aap project.

```bash

VAR_SHA=`date +"%Y-%m-%d %H:%M:%S"`

# change the project.id to correct aap project
cat << EOF > payload.json
{
  "ref": "refs/heads/main",
  "user_name": "Curl Manual Tester",
  "project": {
    "id": 41,
    "name": "service.now.demo.project",
    "web_url": "https://aap-aap.apps.cluster-snw5f-1.dynamic.redhatworkshops.io/execution/projects/41/details"
  },
  "commits": [
    {
      "id": "b6568db1bc1dcd7f8b4d5a946b0b91f9dacd7327",
      "message": "This is a test commit sent directly via curl.",
      "timestamp": "$VAR_SHA",
      "author": {
        "name": "Curl Tester",
        "email": "tester@example.com"
      }
    }
  ],
  "gitlab_payload": {
    "playbook_to_run": "job.template/wzh-dummy.test.yml"
  }
}
EOF

curl -X POST \
     -H "Content-Type: application/json" \
     -H "X-Gitlab-Token: $AAP_WEBHOOK_KEY" \
     -H "X-Gitlab-Event: Push Hook" \
     -d '@payload.json' \
     "$AAP_WEBHOOK_URL"

# output should be:
# {"message":"Job queued."}

```

### rest message setting in service.now

The aap webhook is working, next, we will define a workflow in service.now, which will monitor change request's approval, and trigger the aap workflow with parameter.

<!-- <img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-25-45.png" heigth="600px"> -->

First, we define a `outbound rest message`:

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-26-25.png" heigth="600px">

Click `new`

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-26-55.png" heigth="600px">

Give the name like `aap webhook demo`, and webhook url, and define the `POST` method.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-27-42.png" heigth="600px">

For the `POST` method, give it name, webhook url, headers, and payload content. You need the payload content as template, so later, you can use the template later.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-28-55.png" heigth="600px">


Use the payload content blow as example, change the `project.id` to your project id.

```js
{
  "ref": "refs/heads/main",
  "user_name": "Curl Manual Tester",
  "project": {
    "id": 41,
    "name": "service.now.demo.project",
    "web_url": "https://example.com/execution/projects/41/details"
  },
  "commits": [
    {
      "id": "1111111111111111111",
      "message": "This is a test commit sent directly via curl.",
      "timestamp": "${commit_timestamp}",
      "author": {
        "name": "Curl Tester",
        "email": "tester@example.com"
      }
    }
  ],
  "gitlab_payload": {
    "playbook_to_run": "${playbook_to_run}"
  }
}
```

The `${commit_timestamp}` and `${commit_timestamp}` is variables which will can define and replace during workflow in service.now.

### workflow setting in service.now

Now we define a worflow to use the rest message.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-29-52.png" heigth="600px">

Create `new workflow`

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-30-09.png" heigth="600px">

Give the workflow a `name`, and monitor the table `change request`.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-32-21.png" heigth="600px">

Define run the workflow `alwasy`, and condition set the `approval` `is` `approved`.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-32-48.png" heigth="600px">

In the workflow, add a step `run script`

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-30-53.png" heigth="600px">

Give the step `run script` a name, and set the script.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-34-34.png" heigth="600px">


For the source code, you can use following as example.

```js
(function() {
    /**
     * 此函数用于从工作日志中解析 playbook 的路径。
     * 它会从最新的日志开始，逐行查找关键字 "playbook_to_run:"。
     * @returns {string|null} - 如果找到，则返回 playbook 路径；否则返回 null。
     */
    function getPlaybookFromWorkNotes() {
        // 获取所有工作日志条目，-1代表获取全部。返回的是一个长字符串，各项由 \n\n 分隔。
        var workNotes = current.work_notes.getJournalEntry(-1);
        
        // 按行分割日志内容
        var lines = workNotes.split('\n');

        // 从最新的日志开始，逐行遍历
        for (var i = 0; i < lines.length; i++) {
            var currentLine = lines[i].trim(); // 清除行首尾的空格

            // 检查当前行是否以 "playbook_to_run:" 开头（不区分大小写）
            if (currentLine.toLowerCase().startsWith('playbook_to_run:')) {
                // 找到了关键字，现在提取冒号后面的值
                var parts = currentLine.split(':');
                // 移除第一部分（即关键字 "playbook_to_run"），然后将剩余部分重新组合
                // 这样做是为了防止 playbook 路径本身也包含冒号
                parts.shift(); 
                var playbookPath = parts.join(':').trim(); // 重新组合并清除首尾空格
                
                // 确保提取到的路径不为空
                if (playbookPath) {
                    workflow.info("在 Work Notes 中找到 Playbook: " + playbookPath);
                    return playbookPath; // 返回找到的路径
                }
            }
        }
        
        // 如果遍历完所有行都没有找到，则返回 null
        workflow.warn("在 Work Notes 中未找到 'playbook_to_run:' 关键字。");
        return null;
    }

    // --- 主执行逻辑 ---
    var playbookToRun = getPlaybookFromWorkNotes();

    // 只有在成功提取到 playbook 路径时，才执行 Webhook 调用
    if (playbookToRun) {
        try {
            var r = new sn_ws.RESTMessageV2('aap webhook demo', 'aap post');

            // r.setStringParameterNoEscape('playbook_to_run', current.variables.script_to_run); 

            var currentDateTime = new GlideDateTime().getValue(); // Gets current time in 'yyyy-MM-dd HH:mm:ss' format
            r.setStringParameterNoEscape('commit_timestamp', currentDateTime);

            
            // 关键：将从 Work Notes 中提取的 playbook 路径作为 'script_to_run' 参数的值
            r.setStringParameterNoEscape('playbook_to_run', playbookToRun);
            
            // 在您的 REST Message 中，也需要有 ${commit_timestamp} 这个变量
            r.setStringParameterNoEscape('commit_timestamp', new GlideDateTime().getValue());

            var response = r.execute();
            var httpStatus = response.getStatusCode();

            if (httpStatus == 200) {
                workflow.info("成功发送数据到 Webhook，Playbook: " + playbookToRun);
            } else {
                workflow.error("发送数据到 Webhook 失败。状态码: " + httpStatus);
            }

        } catch (ex) {
            var message = ex.getMessage();
            workflow.error("调用 Webhook 时发生异常: " + message);
        }
    } else {
        // 如果没有找到 playbook，在这里可以定义相应的处理逻辑，例如结束工作流或设置状态
        workflow.info("因未在 Work Notes 中找到 playbook，Webhook 调用被跳过。");
    }
})();
```

### testing

Ok, we defined the workflow in service.now, and we can test it out.

Create a new change request.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-40-27.png" heigth="600px">

Select a type of change request.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-42-31.png" heigth="600px">

Give it a name, and add `work notes`, for example `playbook_to_run: 3344/5566.yml`

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-48-24.png" heigth="600px">

Find the change request created and open it.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-48-47.png" heigth="600px">

Click on the change requet.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-49-14.png" heigth="600px">

Assign to a group, and click `request approval`

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-49-44.png" heigth="600px">

For testing, as admin, we can select an user to approve it.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-50-32.png" heigth="600px">

Ok, the webhook is triggered, and we can see the webhook detail in aap, and we can see the parameter is got.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-51-25.png" heigth="600px">

And the workflow runs smoothly.

<img src="imgs/2025.05.ansible.eda.ai.md/2025-06-12-21-51-38.png" heigth="600px">


# end